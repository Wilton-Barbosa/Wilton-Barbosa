#include <stdio.h>
#include <stdlib.h>
 
int main(){
 
    int X;
    float Y;
 
    // Guarda Valor
    for(X = 0; X < 100; X++){
       scanf("%f",&Y);
 
    // Mostrar valor no intervalo
     if(Y <= 10)
       printf("A[%d] = %.1f\n",X, Y);
     }
      
  
    return 0;
}
